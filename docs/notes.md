* Allow to pass the ``skip`` parameter in ``upload_folder``. The parameter allows to
  skip the upload of matching files
* Allow to pass the ``content_types`` dictionary for mapping subfixes to content-types
